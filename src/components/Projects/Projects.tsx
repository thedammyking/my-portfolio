import React from 'react';

import ContentContainer from '../ContentContainer';

const Projects = () => {
  return (
    <ContentContainer id='projects' label='Projects'>
      <div className='h-80'>Projects</div>
    </ContentContainer>
  );
};

export default Projects;
